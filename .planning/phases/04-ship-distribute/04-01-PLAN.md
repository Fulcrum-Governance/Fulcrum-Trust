---
phase: 04-ship-distribute
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - CONTRIBUTING.md
  - docs/api-reference.md
  - docs/blog-trust-circuit-breaker.md
  - README.md
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "CHANGELOG.md exists with v0.1.0 section in Keep a Changelog 1.1.0 format"
    - "CONTRIBUTING.md exists with dev setup, test, lint, and PR instructions"
    - "docs/api-reference.md exists covering all 6 public classes with docstring references"
    - "docs/blog-trust-circuit-breaker.md has status: published (not draft)"
    - "README.md links to docs/api-reference.md in the docs section"
    - "mypy fulcrum_trust/ reports zero errors in strict mode"
    - "pytest passes with >=95% coverage after polish"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Version history, v0.1.0 Added section"
      contains: "## [0.1.0]"
    - path: "CONTRIBUTING.md"
      provides: "Contributor onboarding guide"
      contains: "pip install -e"
    - path: "docs/api-reference.md"
      provides: "API reference for all public classes"
      contains: "TrustManager"
    - path: "docs/blog-trust-circuit-breaker.md"
      provides: "Published blog post"
      contains: "status: published"
  key_links:
    - from: "README.md"
      to: "docs/api-reference.md"
      via: "markdown link"
      pattern: "api-reference"
    - from: "docs/api-reference.md"
      to: "fulcrum_trust source modules"
      via: "mkdocstrings ::: syntax"
      pattern: ":::"
---

<objective>
Polish the package for v0.1.0 release: write CHANGELOG.md and CONTRIBUTING.md, create docs/api-reference.md using mkdocstrings syntax, mark the blog post as published, update README with docs link, and run a final mypy + pytest clean pass.

Purpose: A publishable package needs more than passing tests — it needs a changelog that communicates what changed, contributor docs that enable community participation, and API reference docs so users know what they're calling. These are the human-facing artifacts that distinguish a real release from an artifact upload.

Output: CHANGELOG.md, CONTRIBUTING.md, docs/api-reference.md, blog post status changed to published, README updated, all quality gates green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ship-distribute/04-RESEARCH.md
@pyproject.toml
@README.md
@docs/blog-trust-circuit-breaker.md
@fulcrum_trust/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md and CONTRIBUTING.md</name>
  <files>CHANGELOG.md, CONTRIBUTING.md</files>
  <action>
Create CHANGELOG.md at repo root in Keep a Changelog 1.1.0 format:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.0] - 2026-02-19

### Added

- Beta distribution trust model (`TrustEvaluator`) with configurable alpha/beta priors
- `TrustManager` orchestrating evaluation, storage, and time-decay in a single interface
- Exponential decay toward uninformative prior (configurable half-life, default 24h)
- `MemoryStore` (default, in-process) and `FileStore` (JSON-backed, cross-session persistence)
- Abstract `TrustStore` Protocol for custom store implementations
- LangGraph adapter (`TrustAwareGraph`) wrapping existing `StateGraph` graphs with zero changes to graph code
- `TrustOutcome.PARTIAL` for fractional trust signals (weighted at 0.5 by default)
- Circuit breaker: `should_terminate()` returns `True` below configurable threshold (default 0.3)
- `TrustConfig` for threshold, half-life, and outcome weight configuration
- Three runnable demos: gratitude loop, drift detection, and recovery scenarios
- 97 tests, 96.83% coverage, mypy strict clean, zero runtime dependencies

[Unreleased]: https://github.com/Fulcrum-Governance/fulcrum-trust/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/Fulcrum-Governance/fulcrum-trust/releases/tag/v0.1.0
```

Create CONTRIBUTING.md at repo root:

```markdown
# Contributing to fulcrum-trust

Thank you for your interest in contributing. This document covers dev setup, running tests, and the PR process.

## Development Setup

```bash
# Clone and install in editable mode with dev dependencies
git clone https://github.com/Fulcrum-Governance/fulcrum-trust.git
cd fulcrum-trust
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
```

## Running Tests

```bash
pytest                  # all 97 tests with coverage (must be >=95%)
pytest --tb=short       # shorter tracebacks
pytest tests/test_manager.py -v  # single module
```

## Type Checking

```bash
mypy fulcrum_trust/     # must report: Success: no issues found
```

## Linting and Formatting

```bash
ruff check .            # lint (must be zero errors)
ruff format .           # auto-format
ruff format --check .   # verify format without writing (used in CI)
```

## Pull Request Guidelines

- Keep PRs focused on a single concern
- All tests must pass: `pytest`
- Zero mypy errors: `mypy fulcrum_trust/`
- Zero ruff errors: `ruff check .`
- Add tests for any new behavior
- Update docstrings for any changed public API

## Code Conventions

- **Type hints everywhere** — mypy strict, no `Any` without justification
- **Docstrings** — Google style on all public classes/methods
- **Imports** — absolute imports only (`from fulcrum_trust.types import TrustState`)
- **Python 3.9+** — use `from __future__ import annotations` in all modules

## Reporting Issues

Open an issue at https://github.com/Fulcrum-Governance/fulcrum-trust/issues. Include:
- Python version and OS
- Minimal reproducible example
- Expected vs. actual behavior
```
  </action>
  <verify>
    Run: `test -f CHANGELOG.md && grep "## [0.1.0]" CHANGELOG.md && echo "CHANGELOG OK"`
    Run: `test -f CONTRIBUTING.md && grep "pip install -e" CONTRIBUTING.md && echo "CONTRIBUTING OK"`
  </verify>
  <done>CHANGELOG.md exists with v0.1.0 section containing all major additions. CONTRIBUTING.md exists with dev setup, test/lint/typecheck commands, and PR guidelines. Both files are at repo root.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/api-reference.md and update blog + README</name>
  <files>docs/api-reference.md, docs/blog-trust-circuit-breaker.md, README.md</files>
  <action>
Create docs/api-reference.md using mkdocstrings ::: syntax. This file documents all 6 public classes from fulcrum_trust/__init__.py: TrustManager, TrustState, TrustOutcome, TrustConfig, MemoryStore, FileStore, plus TrustAwareGraph from the adapter.

Write docs/api-reference.md as a standalone Markdown reference that works without running mkdocs (i.e., include the actual class descriptions and method signatures inline as well as the ::: directives, so the file is readable as plain Markdown on GitHub). Format:

```markdown
# API Reference

This document covers all public classes and methods in `fulcrum-trust`. All classes are importable directly from `fulcrum_trust`.

## TrustManager

The primary interface. Orchestrates trust evaluation, storage, and decay.

::: fulcrum_trust.manager.TrustManager
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

**Key methods:**
- `record_outcome(agent_a, agent_b, outcome)` — Record an interaction result
- `get_trust_score(agent_a, agent_b)` — Current trust score (0.0–1.0, starts at 0.5)
- `should_terminate(agent_a, agent_b)` — True if trust is below threshold
- `get_trust_state(agent_a, agent_b)` — Full TrustState with alpha/beta/timestamp

## TrustState

::: fulcrum_trust.types.TrustState
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

Immutable dataclass holding the Beta distribution parameters for an agent pair:
`alpha` (positive evidence), `beta_val` (negative evidence), `last_updated` (timestamp).

## TrustOutcome

::: fulcrum_trust.types.TrustOutcome
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

Enum with three values: `SUCCESS` (1.0 weight), `PARTIAL` (0.5 weight), `FAILURE` (0.0 weight).

## TrustConfig

::: fulcrum_trust.types.TrustConfig
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

Configuration dataclass with fields:
- `threshold` (default 0.3) — trust score below which `should_terminate()` returns True
- `half_life_seconds` (default 86400) — time for trust to decay halfway toward 0.5
- `alpha_weight` (default 0.2) — weight applied to SUCCESS outcomes
- `partial_beta_weight` (default 0.8) — weight applied to PARTIAL outcome's negative signal

## Stores

### MemoryStore

::: fulcrum_trust.stores.memory.MemoryStore
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

In-process dict-backed store. Default when creating `TrustManager()`. Does not persist across process restarts.

### FileStore

::: fulcrum_trust.stores.file.FileStore
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

JSON file-backed store. Pass a file path: `FileStore("trust_state.json")`. Persists across process restarts. Thread-safe via file locking.

## LangGraph Adapter

### TrustAwareGraph

::: fulcrum_trust.adapters.langgraph.TrustAwareGraph
    options:
      docstring_style: google
      show_root_heading: true
      show_source: false

Wraps a compiled LangGraph `StateGraph` with automatic trust evaluation at node boundaries. Zero changes to existing graph code.

**Usage:**

```python
from fulcrum_trust import TrustManager
from fulcrum_trust.adapters.langgraph import TrustAwareGraph

trust = TrustManager()
trusted_graph = TrustAwareGraph(compiled_graph, trust)
result = trusted_graph.invoke(state)
```
```

Update docs/blog-trust-circuit-breaker.md: change the frontmatter field `status: draft` to `status: published`. Do not change any other content.

Update README.md: add a "Documentation" section (or update the existing docs reference if one exists) that links to docs/api-reference.md. Add before the Contributing section:

```markdown
## Documentation

- [API Reference](docs/api-reference.md) — all public classes and methods
- [Blog post](docs/blog-trust-circuit-breaker.md) — why agents need circuit breakers
```

If a Documentation section already exists in README.md, update it to include both links rather than adding a duplicate section.
  </action>
  <verify>
    Run: `test -f docs/api-reference.md && grep "TrustManager" docs/api-reference.md && echo "API REF OK"`
    Run: `grep "status: published" docs/blog-trust-circuit-breaker.md && echo "BLOG STATUS OK"`
    Run: `grep "api-reference" README.md && echo "README LINK OK"`
  </verify>
  <done>docs/api-reference.md exists covering all 7 public classes (TrustManager, TrustState, TrustOutcome, TrustConfig, MemoryStore, FileStore, TrustAwareGraph) with ::: mkdocstrings directives and inline descriptions. Blog post frontmatter shows status: published. README links to docs/api-reference.md.</done>
</task>

<task type="auto">
  <name>Task 3: Final quality gate pass</name>
  <files></files>
  <action>
Run the full pre-ship quality gate in this exact order. All commands must pass with zero errors before this task is complete.

```bash
# 1. Type check (strict mode, zero errors required)
mypy fulcrum_trust/ --no-error-summary

# 2. Lint (zero errors required)
ruff check .

# 3. Format check (zero diff required)
ruff format --check .

# 4. Full test suite with coverage gate (97 tests, >=95% required)
pytest --tb=short

# 5. Build artifact and validate metadata
rm -rf dist/ build/
python -m build
twine check dist/*
```

If any command fails:
- mypy errors: fix type annotation issues in the identified files
- ruff errors: fix the flagged lint issues (usually unused imports or style violations)
- ruff format --check fails: run `ruff format .` then re-check
- pytest fails: investigate the specific test failure; coverage below 95% requires adding tests
- twine check fails: the most common cause is README rendering issues; check pyproject.toml readme field

Do NOT proceed until all 5 commands pass cleanly. Capture twine check output — "PASSED" on both .whl and .tar.gz is the required outcome.
  </action>
  <verify>
    All 5 commands exit 0:
    - `mypy fulcrum_trust/ --no-error-summary` → "Success: no issues found"
    - `ruff check .` → no output (zero errors)
    - `ruff format --check .` → no output (zero diff)
    - `pytest --tb=short` → "97 passed", coverage >=95%
    - `twine check dist/*` → "PASSED" for both .whl and .tar.gz
  </verify>
  <done>All quality gates pass: mypy strict clean, ruff clean, 97 tests pass with >=95% coverage, dist/ contains both .whl and .tar.gz, twine check reports PASSED for both files.</done>
</task>

</tasks>

<verification>
After all three tasks complete:
1. `ls CHANGELOG.md CONTRIBUTING.md docs/api-reference.md` — all three files exist
2. `grep "## \[0.1.0\]" CHANGELOG.md` — version section present
3. `grep "status: published" docs/blog-trust-circuit-breaker.md` — blog is marked published
4. `grep "api-reference" README.md` — README links to API reference
5. `mypy fulcrum_trust/ --no-error-summary` — zero mypy errors
6. `pytest --tb=short` — all 97 tests pass, >=95% coverage
7. `twine check dist/*` — PASSED for both distribution files
</verification>

<success_criteria>
- CHANGELOG.md exists at repo root with v0.1.0 section in Keep a Changelog format
- CONTRIBUTING.md exists at repo root with dev setup and PR process
- docs/api-reference.md exists covering all 7 public classes
- docs/blog-trust-circuit-breaker.md has status: published
- README.md links to docs/api-reference.md
- All quality gates pass (mypy, ruff, pytest >=95%, twine check PASSED)
- dist/ contains fulcrum_trust-0.1.0-py3-none-any.whl and fulcrum_trust-0.1.0.tar.gz
</success_criteria>

<output>
After completion, create `.planning/phases/04-ship-distribute/04-01-SUMMARY.md`
</output>
